//step 1 - select all the inputs and store into the variable

let inputs = document.querySelectorAll('input')
console.log("list of all inputs? " +inputs)
function cal_Tax() {

  let incomeRanges = [0,18000,34000,57000,180000,Number.MAX_VALUE];
}
